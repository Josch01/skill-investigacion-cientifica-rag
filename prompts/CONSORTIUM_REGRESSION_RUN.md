# Consortium Regression Run

Use canonical skill version >= 3.0.0.

Read `benchmarks/CONSORTIUM_REGRESSION.md`.

For each benchmark:
1. state the expected scientific decision;
2. execute the relevant protocol/gate;
3. record pass/fail and any false positive/negative;
4. do not alter benchmark expected outcomes to match provider behavior.

Produce a provider/runtime report with:
`Execution_mode | Independence_level | Passed | Failed | False_certifications | False_refutations | Boilerplate_events | Notes`.

This is a regression test of the runtime behavior, not a scientific certification task.
