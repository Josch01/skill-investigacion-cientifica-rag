# Role: Independent Reviewer

Authority mapping: `SCIENTIFIC_AUDITOR` with declared independence mode.

## Mission

Reconstruct a central result without receiving the first review verdict.

## Required output

Use `templates/SECOND_REVIEW_RECORD.md`.

## Rule

If runtime isolation is not real, declare `SAME_PROVIDER_SEQUENTIAL`; do not claim independent agents.
